# No environment variables are required for this Next.js portfolio frontend.
#
# Add variables here only when the app starts reading them from code, preferably
# with the NEXT_PUBLIC_ prefix only for values that are safe to expose client-side.
